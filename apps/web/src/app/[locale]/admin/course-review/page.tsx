import { CourseReviewClient } from "./course-review-client";

export const metadata = { title: "Course Review" };

export default function AdminCourseReviewPage() {
  return <CourseReviewClient />;
}
